# Loveable UI Import Plan (Option B)

## Current State
- Dashboard has basic company setup form and NBA checklist
- No shadcn-ui components installed
- Loveable source at `/Users/Shared/Projects/loveable-import/` has 49 UI components

## Implementation Plan

### Step 1: Install Tailwind + shadcn-ui Dependencies
- Install Tailwind CSS: `tailwindcss`, `postcss`, `autoprefixer`
- Install shadcn dependencies: `@radix-ui/react-*`, `class-variance-authority`, `clsx`, `tailwind-merge`, `lucide-react`
- Initialize tailwind.config.js and postcss.config.js
- Replace globals.css with Tailwind directives

### Step 2: Import 49 UI Components (Issue #30)
Copy components from `loveable-import/src/components/ui/` to checklist `src/components/ui/`:
- **Navigation**: sidebar, sheet, drawer, resizable, scroll-area, navigation-menu, menubar, breadcrumb
- **Inputs**: input, textarea, select, form, input-otp, checkbox, radio-group, switch, toggle, toggle-group
- **Display**: button, badge, card, progress, skeleton, avatar, alert, alert-dialog, toast, toaster, sonner, tooltip, popover, hover-card
- **Layout**: dialog, table, tabs, accordion, collapsible, label, separator
- **Interactive**: slider, calendar, dropdown-menu, context-menu, command
- **Media**: carousel, aspect-ratio, chart
- **Utility**: pagination

### Step 3: Dashboard Redesign (Issue #29)
Build new dashboard at `src/app/dashboard/page.tsx` with:
- MetricCard component (label, value, change, icon)
- Badge component (status indicators)
- Progress component (health/status bars)
- Platform modules list with navigation
- Strategic priorities section
- Quick actions section
- Activity timeline
- Lucide icons for all UI elements

### Step 4: Next Feature Pages
- Issue #21: Intelligence Page
- Issue #22: Lead Generation Page
- Issue #23: Content Creation Page
- Issue #24: CRM Page
- Issue #25: Strategy Page
- Issue #26: Execution Page
- Issue #27: Portfolio Page
- Issue #28: Brand Page

## Dependencies
- Need to check if checklist has tailwind configured
- Need to add lib/utils.ts for cn() helper

# Relocation Package Plan

## User Request
User is moving to another computer. Need to:
1. Commit and push all code to GitHub
2. Create zip file with environment needed to run on new machine
3. Create MD file with instructions for agent on other side

---

## Implementation

### Step 1: Commit and Push
```bash
cd /Users/Shared/Projects/checklist
git add agents/ .kilo/
git commit -m "feat(agents): Add Paperclip agent prompts, scripts, and implementation plan"
git push origin main
```

### Step 2: Create Relocation Package
Contents for zip:
- All source code (src/, prisma/, agents/)
- Configuration files (package.json, next.config.js, tsconfig.json, vercel.json, .env.example)
- Documentation (README.md, SPEC.md, IDEABANK.md, LOVEABLE_IMPORT.md)
- Scripts (scripts/)
- Kilo config (.kilo/)
- .git folder (for git history)

### Step 3: Create README for New Machine
File: `RELOCATION_README.md`
- How to install dependencies
- Environment variables needed
- Database setup (Neon)
- How to run locally
- Vercel deployment notes