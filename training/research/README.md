# Parked Research

These tools are intentionally not part of the active checklist delivery plan today:

- `Unsloth`
- `LLaMA-Factory`
- `Axolotl`

Reason:

- checklist is Apple Silicon only
- the current production path should stay aligned with Apple-native MLX / MLX-LM training and Ollama runtime deployment

These tools may be re-evaluated later, but they should not be counted as active implementation scope right now.
